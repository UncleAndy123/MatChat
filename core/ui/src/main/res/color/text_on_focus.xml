<?xml version="1.0" encoding="utf-8"?>
<!-- Primary text. The focus highlight is now a LIGHT fill (surface_focused), so
     text stays dark in every state and is always legible. Kept as a state list
     so callers/roles are unchanged. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:color="@color/text_primary" />
</selector>
